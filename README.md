# 📱 מערכת אוטומציה של WhatsApp

מערכת אוטומציה מלאה לשליחת הודעות WhatsApp דרך Node.js באמצעות `whatsapp-web.js`.

## 🚀 התחלה מהירה

### שלב 1: התקנה (כבר בוצעה!)
```bash
npm install
```

### שלב 2: אימות ראשוני
בפעם הראשונה שאתה מריץ את הסקריפט, תראה QR Code. סרוק אותו עם WhatsApp:

1. פתח WhatsApp במכשיר שלך
2. לך ל**הגדרות** → **מכשירים מקושרים** → **קשר מכשיר**
3. סרוק את ה-QR Code שמופיע בטרמינל

הסשן נשמר אוטומטית בתיקיית `.wwebjs_auth/`, אז לא תצטרך לסרוק שוב!

---

## 📝 סקריפטים זמינים

### 1️⃣ שליחת הודעה פשוטה
ערוך את `sendMessage.js` ושנה את המספר וההודעה:

```javascript
const phoneNumber = '972501234567'; // המספר שלך (ללא +)
const message = 'ההודעה שלך כאן!';
```

הרץ:
```bash
node sendMessage.js
```

---

### 2️⃣ שליחת הודעה דרך Command Line
שלח הודעה ישירות מה-terminal:

```bash
node sendMessageCLI.js 972501234567 "שלום מקלוד!"
```

**פורמט מספר טלפון:**
- ישראל: `972` + מספר ללא 0 (דוגמה: `972501234567`)
- ארה"ב: `1` + מספר (דוגמה: `12125551234`)
- בריטניה: `44` + מספר (דוגמה: `447911123456`)

---

### 3️⃣ תכונות מתקדמות
הסקריפט `advancedFeatures.js` כולל:
- ✅ שליחת הודעות עם emojis
- ✅ הודעות מרובות שורות
- ✅ שליחת תמונות/קבצים
- ✅ שליחת מיקום
- ✅ בדיקת סטטוס מספר
- ✅ קבלת מידע על צ'אט

```bash
node advancedFeatures.js
```

---

### 4️⃣ שליחה המונית חכמה (BULK SENDER) 🚀

הסקריפט `bulkSender.js` מאפשר שליחה **טבעית ואורגנית** למספר נמענים:

**תכונות מיוחדות:**
- ⏱️ **מרווחי זמן אקראיים טבעיים** (30-60 שניות בין הודעות) - **בטוח ומומלץ!**
- 📝 **התאמה אישית אוטומטית** של הודעות עם placeholders
- ✅ **בדיקת תקינות מספרים** לפני שליחה
- 📊 **דוח מפורט** של כל השליחות
- 🔄 **תמיכה ב-JSON וב-CSV**

**⚠️ אזהרות אבטחה חשובות:**
- WhatsApp חוסם חשבונות ששולחים **100+ הודעות תוך 5 דקות**
- מומלץ **מרווח של 30-60 שניות** בין הודעות (כבר מוגדר!)
- **חימום חשבון** (Account Warming) למספרים חדשים/לא פעילים:
  - שבוע 1: 100-200 הודעות ליום
  - שבוע 2: 500-1,000 הודעות ליום
  - לאחר מכן: עד 1,500 הודעות ליום (במרווחים!)
- **אל תשתמש ב-`--no-delay`** אלא אם אתה יודע מה אתה עושה!

**שימוש:**
```bash
# שליחה בסיסית
node bulkSender.js contacts.json message-template.txt

# שליחה עם בדיקת מספרים
node bulkSender.js contacts.csv message-template.txt --validate
```

**פורמט קובץ אנשי קשר (JSON):**
```json
[
    {
        "name": "יוסי כהן",
        "firstName": "יוסי",
        "phone": "972501234567",
        "company": "חברת הייטק",
        "customField": "לקוח VIP"
    }
]
```

**פורמט קובץ אנשי קשר (CSV):**
```csv
name,firstName,lastName,phone,company,customField
יוסי כהן,יוסי,כהן,972501234567,חברת הייטק,לקוח VIP
```

**תבנית הודעה עם placeholders:**
```
שלום {firstName}! 👋

אני מקווה שהכל בסדר ב-{company}.

רציתי לעדכן אותך...
```

**Placeholders נתמכים:**
- `{name}` - שם מלא
- `{firstName}` - שם פרטי
- `{lastName}` - שם משפחה
- `{company}` - חברה
- `{customField}` - שדה מותאם אישית

---

### 5️⃣ מנהל רשימות תפוצה אינטראקטיבי 📋

הסקריפט `listManager.js` מאפשר ניהול נוח של רשימות:

```bash
node listManager.js
```

**תפריט:**
1. יצירת רשימה חדשה
2. הוספה לרשימה קיימת
3. הצגת רשימה
4. ייצוא ל-CSV
5. יציאה

**דוגמאות שימוש:**
- צור רשימת "לקוחות VIP"
- הוסף אנשי קשר בצורה אינטראקטיבית
- ייצא ל-CSV לשימוש ב-bulkSender

---

## 🤖 שימוש מ-Claude Code

אני (Claude) יכול להריץ את הסקריפטים האלה עבורך!

**דוגמה:**
```
אלעד: "שלח הודעה ב-WhatsApp למספר 972501234567 עם הטקסט 'שלום עולם'"
קלוד: *מריץ את הסקריפט עם הפרמטרים שלך*
```

פשוט תבקש ממני, ואני אפעיל את הסקריפט המתאים!

---

## 📚 דוגמאות קוד

### שליחת הודעה מתוך קוד אחר
```javascript
const { Client, LocalAuth } = require('whatsapp-web.js');

async function sendWhatsApp(phoneNumber, message) {
    const client = new Client({
        authStrategy: new LocalAuth()
    });

    client.on('ready', async () => {
        const chatId = phoneNumber + '@c.us';
        await client.sendMessage(chatId, message);
        await client.destroy();
    });

    client.initialize();
}

// שימוש
sendWhatsApp('972501234567', 'היי!');
```

### שליחת תמונה
```javascript
const { MessageMedia } = require('whatsapp-web.js');

// מקובץ מקומי
const media = MessageMedia.fromFilePath('./image.jpg');
await client.sendMessage(chatId, media, {
    caption: 'כיתוב לתמונה'
});

// מ-URL
const media2 = await MessageMedia.fromUrl('https://example.com/image.jpg');
await client.sendMessage(chatId, media2);
```

### שליחת הודעה לקבוצה
```javascript
// קבל את כל הצ'אטים
const chats = await client.getChats();

// מצא קבוצה לפי שם
const group = chats.find(chat =>
    chat.isGroup && chat.name === 'שם הקבוצה'
);

// שלח הודעה
await group.sendMessage('שלום לכולם!');
```

### האזנה להודעות נכנסות
```javascript
client.on('message', async (msg) => {
    console.log('הודעה התקבלה:', msg.body);

    // השב אוטומטית
    if (msg.body === 'היי') {
        await msg.reply('שלום! איך אני יכול לעזור?');
    }
});
```

---

## ⚙️ הגדרות מתקדמות

### שינוי headless mode (להצגת דפדפן)
ב-`sendMessage.js`, שנה:
```javascript
puppeteer: {
    headless: false, // יציג את הדפדפן
    // ...
}
```

### שימוש במספר סשנים
שנה את ה-`clientId`:
```javascript
authStrategy: new LocalAuth({
    clientId: "client-two" // סשן שני
})
```

---

## 🔒 אבטחה

- ⚠️ **לעולם אל תעלה את התיקייה `.wwebjs_auth/` ל-Git!**
- ⚠️ **שמור על המשתמש שלך מחובר בטלפון**
- ⚠️ **אל תשתף את ה-session data עם אף אחד**

קובץ `.gitignore` כבר נוצר עבורך.

---

## ❓ פתרון בעיות

### QR Code לא מוצג?
וודא שהטרמינל שלך תומך ב-UTF-8. נסה להריץ ב-Windows Terminal או VSCode terminal.

### "Session terminated"
מחק את התיקייה `.wwebjs_auth/` והתחבר מחדש:
```bash
rm -rf .wwebjs_auth
node sendMessage.js
```

### "Phone number is not registered"
המספר לא רשום ב-WhatsApp, או שהפורמט שגוי. וודא:
- ללא סימן `+`
- ללא רווחים או מקפים
- כולל קוד מדינה (972 לישראל)

---

## 📖 מקורות

- [whatsapp-web.js Documentation](https://docs.wwebjs.dev/)
- [GitHub Repository](https://github.com/pedroslopez/whatsapp-web.js)

---

## 🎉 נוצר על ידי Claude Code

מערכת זו נוצרה אוטומטית על ידי Claude - העוזר האישי שלך לפיתוח!

**צריך עזרה?** פשוט שאל את Claude: "Claude, שלח הודעת WhatsApp ל-..."
