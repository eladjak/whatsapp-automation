# WhatsApp Automation - Environment Configuration
# Copy this file to .env and adjust values as needed.
# All variables are optional; defaults are shown below.

# --- Delay Settings (milliseconds) ---
# Minimum delay between messages (default: 30000 = 30 seconds)
WA_DELAY_MIN=30000
# Maximum delay between messages (default: 60000 = 60 seconds)
WA_DELAY_MAX=60000
# Extra minimum delay for personalized messages (default: 5000 = 5 seconds)
WA_DELAY_PERSONALIZED_MIN=5000
# Extra maximum delay for personalized messages (default: 10000 = 10 seconds)
WA_DELAY_PERSONALIZED_MAX=10000

# --- Client Settings ---
# WhatsApp client session ID (default: "default")
WA_CLIENT_ID=default
# Run browser in headless mode (default: true)
WA_PUPPETEER_HEADLESS=true

# --- Logging ---
# Log level: silent, error, warn, info, debug (default: info)
WA_LOG_LEVEL=info
